{
  "name": "po-management-beta", "version": "0.1.0", "private": true, "type": "module",
  "scripts": {"dev":"wrangler dev", "deploy":"wrangler deploy", "db:local":"wrangler d1 migrations apply DB --local", "db:remote":"wrangler d1 migrations apply DB --remote", "test":"node --test tests/*.test.mjs"},
  "devDependencies": {"wrangler":"^4.0.0"}
}
